#!/usr/bin/env python3
# This file is placed in the Public Domain.
# pylint: disable=C,I,R,W0212
# ruff: noqa: E402


"service"


import getpass
import os


from rssbot.config  import Config
from rssbot.main    import init, scan, wrap
from rssbot.utils   import forever, pidfile, privileges
from rssbot.modules import all as modules


Cfg = Config()


def main():
    "main"
    Cfg.mod = "cmd,err,irc,rss,thr"
    Cfg.user = getpass.getuser()
    privileges(Cfg.user)
    pidfile(Cfg.pidfile)
    scan(Cfg.mod, modules)
    init(Cfg.mod, modules)
    forever()


if __name__ == "__main__":
    wrap(main)
    os._exit(0)
