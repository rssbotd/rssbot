#!/usr/bin/env python3
# This file is placed in the Public Domain.
# pylint: disable=C0413,W0611


"prompt"


import os
import readline
import sys
import termios


sys.path.insert(0, os.getcwd())


from rssbot.config  import Config


Cfg = Config()
Cfg.mod = "cmd,err,mod,thr"


from rssbot.console import Console
from rssbot.errors  import errors
from rssbot.main    import boot, cmnd, enable, init
from rssbot.modules import face
from rssbot.parse   import parse
from rssbot.utils   import forever


def wrap(func):
    "reset console."
    old3 = None
    try:
        old3 = termios.tcgetattr(sys.stdin.fileno())
    except termios.error:
        pass
    try:
        func()
    except (KeyboardInterrupt, EOFError):
        pass
    finally:
        if old3:
            termios.tcsetattr(sys.stdin.fileno(), termios.TCSADRAIN, old3)


def main():
    "main"
    boot()
    parse(Cfg, " ".join(sys.argv[1:]))
    csl = Console(print, input)
    if "i" in Cfg.opts:
        init(Cfg.mod, face)
    csl.start()
    forever()


if __name__ == "__main__":
    wrap(main)
    errors()
